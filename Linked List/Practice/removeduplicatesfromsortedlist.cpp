#include <bits/stdc++.h>
using namespace std;

// ListNode *deleteDuplicates(ListNode *head)
// {
//     if (head == NULL || head->next == NULL) // Length 0 and 1 ke liye
//         return head;
//     ListNode *a = head, *b = head->next; // Length 2 se 300 ke liye  as given
//     while (b)
//     {
//         while (b != NULL && a->val == b->val)
//             b = b->next;
//         a->next = b;
//         // for next round
//         a = b;
//         if (b != NULL)
//             b = b->next;
//     }
//     return head;
// }

int main()
{
}